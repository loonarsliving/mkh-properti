# MOBILE_BUILD

## Status: NOT IMPLEMENTED

No Capacitor, Cordova, React Native, Android, or iOS project exists in this repository. Searched for `capacitor.config.*`, `*.xcodeproj`, `AndroidManifest.xml`, `android/`, `ios/` directories — none found.

The app is a set of plain HTML pages, presumably accessed via a mobile browser (the app does contain some UI copy suggesting field-staff use on phones, e.g. "Tidak perlu login" no-login forms clearly aimed at quick mobile use), but there is **no native wrapper, no app store packaging, and no mobile build tooling** anywhere in this repo.

Do not add Capacitor/Android/iOS build steps to any documentation or workflow — none currently exist. If mobile packaging is desired in the future, that is new work outside this audit's scope.
